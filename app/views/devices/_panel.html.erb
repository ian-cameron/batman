	  <div class="panel panel-default">
	    <div class="panel-heading">	
	      <h3 class="panel-title">Batteries Available</h3>
		  <%=link_to "<div class='btn-header btn-sm btn-default pull-right'><span class='glyphicon glyphicon-plus'></span> New Battery</div>".html_safe, new_device_path %>
		  <%=link_to "<div class='btn-header btn-sm btn-default pull-right'><span class='glyphicon glyphicon-list'></span> View All</div>".html_safe, devices_path %>
	    </div>
	    <div class="panel-body">
			<div class="project" id="batteries_available">
	      <% @devices.each do |d| %>
            <%=render 'devices/battery', {:d => d}%>
		  <%end%>
	  </div>
	    </div>
	  </div>